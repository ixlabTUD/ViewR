using UnityEngine;

namespace ViewR.Core.OVR.UX
{
    //[RequireComponent(typeof(LaserPointer))]
    public class UILaserBeamBehaviorController : MonoBehaviour
    {
        //[SerializeField]
        //private LaserPointer.LaserBeamBehavior laserBeamBehavior;
        
        //private LaserPointer _laserPointer;


        //private void Awake()
        //{
        //    // Get refs
        //    _laserPointer = GetComponent<LaserPointer>();
        //}

        //private void Start()
        //{
        //    SetLaserBeamBehavior(laserBeamBehavior);
        //}

        ///// <summary>
        ///// Allow us to set it on changes through the inspector
        ///// </summary>
        //private void OnValidate()
        //{
        //    // Bail if not in play mode, or if there is no laser pointer yet.
        //    if (!Application.isPlaying || _laserPointer == null) return;
            
        //    SetLaserBeamBehavior(laserBeamBehavior);
        //}

        //public void SetLaserBeamBehavior(LaserPointer.LaserBeamBehavior newLaserBeamBehavior) =>
        //    _laserPointer.laserBeamBehavior = newLaserBeamBehavior;
    }
}